{
  "name": "folder-info",
  "version": "1.0.4",
  "description": "Obsidian plugin that shows separate file and folder counts in the native File Explorer.",
  "main": "main.js",
  "type": "commonjs",
  "scripts": {
    "build": "node build.mjs",
    "test": "node build.mjs && node --test test.cjs",
    "check": "npm run test && node --check main.js && node verify-version.mjs"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "file-explorer",
    "folders",
    "counts"
  ],
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
