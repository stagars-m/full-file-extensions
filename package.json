{
  "name": "full-file-extensions",
  "version": "1.1.3",
  "description": "Obsidian plugin that keeps file extensions visible beside filenames in the native File Explorer.",
  "main": "main.js",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test.cjs",
    "check": "npm run build && npm test && node --check main.js"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "file-explorer",
    "file-extensions"
  ],
  "license": "MIT",
  "private": true
}
