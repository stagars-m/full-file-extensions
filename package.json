{
  "name": "full-file-extensions",
  "version": "1.0.0",
  "description": "Obsidian plugin that shows complete filenames in the native File Explorer.",
  "main": "main.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "check": "npm run test && npm run build && node --check main.js"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "file-explorer",
    "file-extensions"
  ],
  "license": "MIT",
  "private": true
}
