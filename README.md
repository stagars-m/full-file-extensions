# full-file-extensions
This Obsidian plugin displays complete filenames with file extensions. Uses Obsidian’s native File Explorer and hides the redundant extension badge. Restores native labels when disabled. Makes no network requests. Contains no telemetry, settings, runtime dependencies, or vault writes. Does not read note or attachment contents.
